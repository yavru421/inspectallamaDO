{
  "name": "inspectallamado",
  "version": "1.0.0",
  "description": "InspectaLlama - Next-Gen AI Web Search Engine powered by Cloudflare Durable Objects and Browser Rendering",
  "main": "src/index.ts",
  "scripts": {
    "build": "node -e \"fs.rmSync('dist', {recursive: true, force: true})\" && dotnet publish InspectaLlamaDO.csproj -c Release -o dist && node scripts/postbuild.js",
    "dev": "wrangler dev",
    "deploy": "git push origin main"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^0.0.13"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240725.0",
    "typescript": "^5.5.4",
    "wrangler": "^3.114.17"
  }
}
